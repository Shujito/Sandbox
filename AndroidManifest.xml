<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.shujito.sandbox"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="22" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name="org.shujito.sandbox.SandboxApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name" >
        <activity android:name="org.shujito.sandbox.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.shujito.sandbox.FloatingViewActivity"
            android:label="@string/activity_floating" >
        </activity>
        <activity
            android:name="org.shujito.sandbox.TouhousActivity"
            android:label="@string/activity_touhous" >
        </activity>
        <activity
            android:name="org.shujito.sandbox.SurfaceActivity"
            android:label="@string/activity_surface"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <provider
            android:name="org.shujito.sandbox.Provider"
            android:authorities="org.shujito.sandbox"
            android:exported="false" />

        <service android:name="org.shujito.sandbox.FloatingViewService" >
        </service>
    </application>

</manifest>